__pycache__/
*.pyc
.venv/
.DS_Store
PR_BODY.md
certs/
identities.json
identities/

# arduino-cli build tree — ~31 MB. Regenerated by `make firmware` (~33s cold),
# and it is also the warm object cache that makes a rebuild ~18s. Ignore it,
# do not delete it.
firmware/sketch/build/
