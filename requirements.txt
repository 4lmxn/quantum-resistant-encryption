cryptography>=42
flask>=3
flask-socketio>=5.3
python-socketio[client]>=5.11
kyber-py>=1.2
dilithium-py>=1.1
requests>=2.31

# MQTT over TLS 1.3 transport (report §3.1)
paho-mqtt>=2.1
amqtt>=0.12

# Optional — only for storing the server key in a PKCS#11 HSM (make hsm-import)
# python-pkcs11>=0.9
